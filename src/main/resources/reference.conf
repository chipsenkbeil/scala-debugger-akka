scala-debugger {
  task-runner {
    # Maximum number of virtual machines to use the task manager
    max-virtual-machines = 0 # Non-positive integer results in unbounded limit

    # Maximum threads to spin up per task runner
    threads = 0 # Non-positive integer results in using JVM cores
  }

  # Represents the data to retrieve during an event
  messages {
    # =========================================================================
    # = DEFAULT IMPLEMENTATIONS
    # =========================================================================

    # TODO: How do I indicate that I want to call something like...
    #
    #   stackFrame.getValues(stackFrame.visibleVariables)
    #
    # and add it to the array to create (LocalVariable, Value)?
    #

    # Represents the default type configuration for all messages
    default-type {
      name      = "String"
      signature = "String"
    }

    # Represents the default location configuration for all messages
    default-location = {
      lineNumber  = "Int"
      sourceName  = "String"
      sourcePath  = "String"
    }

    # Represents the default local variable configuration for all messages
    default-local-variable {
      isArgument  = "Boolean"
      name        = "String"
      signature   = "String"
      type        = ${default-type}
    }

    # Represents the default field configuration for all messages
    default-field {
      isTransient = "Boolean"
      isVolatile  = "Boolean"
      type        = ${default-type}
    }

    # Represents the default value configuration for all messages
    default-value {
      type  = ${default-type}
    }

    # Represents the default method configuration for all messages
    default-method {
      allLineLocations  = ${default-location} # TODO: How to apply to array?
      arguments         = ${default-local-variable} # TODO: How to apply to array?
      location          = ${default-location}
      returnType        = ${default-type}
      variables         = ${default-local-variable} # TODO: How to apply to array?
    }

    # Represents the default stack frame configuration for all messages
    default-stack-frame {
      getArgumentValues = ${default-value} # TODO: How to apply to array?
      location          = ${default-location}
      visibleVariables  = ${default-local-variable} # TODO: How to apply to array?
    }

    # Represents the default thread configuration for all messages
    default-thread {
      frames  = ${default-stack-frame} # TODO: How to apply to array?
      name    = "String"
      status  = "Int"
    }

    # =========================================================================
    # = MESSAGE IMPLEMENTATIONS
    # =========================================================================

    access-watchpoint {
      field         = ${default-field}
      valueCurrent  = ${default-value}
    }

    breakpoint {
      thread    = ${default-thread}
      location  = ${default-location}
    }

    class-prepare {

    }

    class-unload {

    }

    exception {

    }

    method-entry {

    }

    method-exit {

    }

    modification-watchpoint {

    }

    step {

    }

    thread-start {

    }

    thread-death {

    }

    vm-start {

    }

    vm-death {

    }

    vm-disconnect {

    }
  }
}